{
  "name": "url-snake",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Luke.Batchelor",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && DIST=\"index\" node run.js && cd golf && DIST=\"golf\" node ../run.js"
  },
  "devDependencies": {
    "uglify-js": "^3.17.4"
  }
}
