{
  "name": "url-snake",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Luke.Batchelor",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && node run.js && cd golf && node ../run.js && cp index.html ../dist/golf.html && cp ../index.html ../dist/index.html"
  },
  "devDependencies": {
    "uglify-js": "^3.17.4"
  }
}
